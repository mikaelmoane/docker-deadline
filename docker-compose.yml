services:
  mongo:
    container_name: mongo
    image: mongo:4.2
    user: 1000:1000
    ports:
      - "27017:27017"
    volumes:
      - /mnt/altair/repository/deadline/DeadlineDatabase10:/data/db
    restart: unless-stopped

  webservice:
    container_name: webservice
    user: 1000:1000
    build: 
      context: .
      dockerfile: deadline-client.Dockerfile
    ports:
      - "8082:8082"
    volumes:
      - /mnt/altair/repository/deadline/DeadlineRepository10:/mnt/DeadlineRepository10
    entrypoint: ./deadlinewebservice -port 8082
    restart: unless-stopped
    depends_on:
      - mongo
